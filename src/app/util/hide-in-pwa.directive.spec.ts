import { HideInPwaDirective } from './hide-in-pwa.directive';

describe('HideInPwaDirective', () => {
  it('should create an instance', () => {
    const directive = new HideInPwaDirective();
    expect(directive).toBeTruthy();
  });
});
